question:我么有一个包含N个元素的元素或序列，现在想将它分解为N个单独的变量


eg.
	p = (4, 5)
	a, b = p

实际上：不仅仅只是元祖和列表，只要对象恰好是可迭代的，那么就可以执行分解操作。包括字符串，文件，迭代器以及生成器。
eg.
	s = 'hello'
	a,b,c,d,e = s


有时候，可能想丢弃某些特定的值，python 并没有提供特定的语法来实现这一点，但是通常可以选一个用不到的变量名，以此来作为要丢弃的值的名称。

eg. 
	data = ['ACME', 50, 91.1, (2012, 12, 20)]
	_, share, price, _ = data


