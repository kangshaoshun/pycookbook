question:从任意长度的可迭代对象中分解元素
	
desc:需要从某个可迭代对象中分解出N个元素，但是这个可迭代对象的长度可能超过N,这会呆滞出现"分解值过多"异常

eg.(python2 不支持)
	record = ('Dave', 'dave@example.com', '773-5551212', '847-555-1212')
	name, email, *phone = record

*式语法在迭代一个编程的元祖序列时尤其有用。

有时候可能想分解除某些值然后蝶祈他们，在分解的时候，不能只是指定一个单独*,但是可以使用几个常用来表示袋丢弃值的变量名，比如_或者ign.
eg.
	record = ('ACME', 50, 123.45, (12, 18, 2012))
	name, *_, (*_, year) = record

